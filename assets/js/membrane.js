(function () { "use strict"; var root = document.documentElement; var KEY = "membrane-theme"; try { var saved = localStorage.getItem(KEY); if (saved === "light") root.setAttribute("data-theme", "light"); } catch (e) {} function toggleTheme() { var isLight = root.getAttribute("data-theme") === "light"; if (isLight) { root.removeAttribute("data-theme"); persist("dark"); } else { root.setAttribute("data-theme", "light"); persist("light"); } } function persist(v) { try { localStorage.setItem(KEY, v); } catch (e) {} } function initReveal() { var els = document.querySelectorAll(".reveal"); var reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches; if (reduce || !("IntersectionObserver" in window)) { els.forEach(function (el) { el.classList.add("is-in"); }); return; } var io = new IntersectionObserver(function (entries) { entries.forEach(function (entry) { if (entry.isIntersecting) { entry.target.classList.add("is-in"); io.unobserve(entry.target); } }); }, { threshold: 0.12, rootMargin: "0px 0px -8% 0px" }); els.forEach(function (el) { io.observe(el); }); } function initLightbox() { var lb = document.getElementById("lightbox"); if (!lb) return; var lbImg = lb.querySelector("img"); var lastFocus = null; function visibleImg(frame) { var imgs = frame.querySelectorAll("img"); for (var i = 0; i < imgs.length; i++) { if (imgs[i].offsetParent !== null) return imgs[i]; } return imgs[0] || null; } function open(img) { lbImg.src = img.currentSrc || img.src; lbImg.alt = img.alt || ""; lb.classList.add("is-open"); lb.setAttribute("aria-hidden", "false"); document.body.style.overflow = "hidden"; lastFocus = document.activeElement; var btn = lb.querySelector(".lightbox__close"); if (btn) btn.focus(); } function close() { lb.classList.remove("is-open"); lb.setAttribute("aria-hidden", "true"); document.body.style.overflow = ""; lbImg.removeAttribute("src"); if (lastFocus && lastFocus.focus) lastFocus.focus(); } document.addEventListener("click", function (e) { if (!e.target || !e.target.closest) return; var frame = e.target.closest(".device__frame"); if (frame && !lb.contains(frame)) { var img = visibleImg(frame); if (img) { e.preventDefault(); open(img); } return; } if (e.target.closest(".lightbox")) close(); }); document.addEventListener("keydown", function (e) { if (e.key === "Escape" && lb.classList.contains("is-open")) close(); }); } function initWaitlist() { var form = document.getElementById("waitlist"); if (!form) return; var done = document.getElementById("waitlist-done"); var hint = form.querySelector(".waitlist__hint"); var sent = false; if (done) done.hidden = true; function showDone() { if (hint) hint.hidden = true; form.hidden = true; if (done) { done.hidden = false; if (done.focus) done.focus(); } } function showHint(msg) { if (done) done.hidden = true; if (!hint) return; hint.textContent = msg; hint.hidden = false; } form.addEventListener("submit", function (e) { e.preventDefault(); var hp = form.querySelector(".waitlist__hp"); if (hp && hp.value) { showDone(); return; } if (sent) return; sent = true; if (hint) hint.hidden = true; var btn = form.querySelector('button[type="submit"]'); if (btn) { btn.disabled = true; btn.textContent = "Adding you…"; } function reset() { sent = false; if (btn) { btn.disabled = false; btn.textContent = "Notify me at launch"; } } var email = (form.querySelector("#wl-email") || {}).value || ""; var body = new URLSearchParams({ email: email }); fetch(form.action, { method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded" }, body: body }) .then(function (r) { return r.json().catch(function () { return {}; }); }) .then(function (data) { if (data && data.ok) { showDone(); } else { if (data && (data.detail || data.status)) { console.warn("waitlist: upstream", data.status || "", data.detail || ""); } reset(); showHint("Hmm — that didn't go through. Try again, or a different email."); } }) .catch(function () { reset(); showHint("Couldn't reach the server — check your connection and try again."); }); }); } function initAttest() { var nodes = document.querySelectorAll("[data-attest]"); if (!nodes.length) return; fetch("attestation.json", { cache: "no-store" }) .then(function (r) { return r.ok ? r.json() : null; }) .then(function (a) { if (!a || !a.corpus || !a.invariants) return; var c = a.corpus, inv = a.invariants; function fmt(k) { if (k === "lines") return Math.floor(c.lines / 10000) * 10 + "K+"; if (k === "theorems") return (Math.floor(c.theorems_plus_lemmas / 1000) * 1000).toLocaleString("en-US") + "+"; if (k === "theorems-plain") return (Math.floor(c.theorems_plus_lemmas / 1000) * 1000).toLocaleString("en-US"); if (k === "files") return c.files.toLocaleString("en-US"); if (k === "axioms") return String(inv.project_axioms); if (k === "digest") return (a.digest && a.digest.root) || ""; return null; } nodes.forEach(function (el) { var v = fmt(el.getAttribute("data-attest")); if (v != null) el.textContent = v; }); }) .catch(function () {}); } function initHomeWorld() { if (!document.getElementById("top") || !document.getElementById("wedge")) return; if (!document.querySelector('script[data-home-world]')) { var world = document.createElement("script"); world.src = "assets/js/home-world.js?v=2"; world.defer = true; world.dataset.homeWorld = "true"; document.head.appendChild(world); } if (!document.querySelector('script[data-home-depth]')) { var depth = document.createElement("script"); depth.src = "assets/js/home-depth.js?v=2"; depth.defer = true; depth.dataset.homeDepth = "true"; document.head.appendChild(depth); } } document.addEventListener("DOMContentLoaded", function () { initReveal(); initLightbox(); initWaitlist(); initAttest(); initHomeWorld(); var t = document.querySelector("[data-theme-toggle]"); if (t) t.addEventListener("click", toggleTheme); }); })();;(function(){document.addEventListener("DOMContentLoaded",function(){if(!document.getElementById("wedge")||!document.getElementById("modes")||document.querySelector("script[data-home-anchors]"))return;var s=document.createElement("script");s.src="assets/js/home-anchors.js?v=1";s.defer=true;s.dataset.homeAnchors="true";document.head.appendChild(s);});})();